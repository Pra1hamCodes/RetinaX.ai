# Minimal deps for the standalone (no Docker / no Postgres / no Redis) backend.
# Used when the user wants to run the backend directly on Windows without the
# full compose stack. SQLite + sync inference.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

pydantic==2.9.2
pydantic-settings==2.5.2

SQLAlchemy==2.0.35
alembic==1.13.3

passlib[bcrypt]==1.7.4
bcrypt==4.0.1
PyJWT==2.9.0

tensorflow==2.15.1
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==10.4.0
scikit-learn==1.5.2
